enum Status { changed, unchanged, newFile, deleted, renamed, moved, conflicted , containsConflicts, containsunusedCode }

enum SetupStatus { notStarted, inProgress, completed, failed , extracted, needsExtraction }